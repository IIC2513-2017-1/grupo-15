<%= form_for([@project, @reward]) do |f| %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount, step: 0.01, min: 1, required: true,
    placeholder: "Monto mínimo de $1" %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
